# Socratic-OT — Streamlit Cloud requirements

# Vector store + embeddings
chromadb==0.5.23
sentence-transformers==3.3.1

# LangChain stack (versions must be mutually compatible)
langchain==0.3.20
langchain-groq==0.2.4
langchain-community==0.3.19
langchain-core==0.3.41
langgraph==0.2.74

# LLM clients
groq==0.13.1
openai==1.65.4

# UI
streamlit>=1.40.0
bcrypt>=4.0.0

# Evaluation (needed for evaluation module imports)
ragas==0.2.14
datasets==3.3.2

# Vision / transformers (no torch pin — cloud pulls compatible version)
transformers==4.49.0
accelerate==1.4.0

# Utilities (unpinned numpy/pandas so pip uses pre-built wheels on Python 3.14)
Pillow>=10.0.0
numpy>=1.26.0
pandas>=2.1.0
tqdm>=4.0.0
plotly>=5.18.0
gtts>=2.5.0
streamlit-audiorecorder>=0.0.5
